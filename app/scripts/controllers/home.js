'use strict';

/**
 * @ngdoc function
 * @name TzUI.controller:HomeCtrl
 * @description # Controller of the TzUI
 */
angular.module('TzUI').controller(
		'HomeCtrl',
		[ '$scope', '$http', '$timeout', '$location', 'gettextCatalog', 'StorageCtrl', 'SessionService',
				'$interval',
				function($scope, $http, $timeout, $location, gettextCatalog, StorageCtrl, SessionService, $interval) {

				} ]);
